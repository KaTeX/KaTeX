{
  "scripts": {
    "prestart": "cp ../docs/cli.md.template ../docs/cli.md && node ../cli.js markdown-help >> ../docs/cli.md && cp -r ../dist/katex.min.css ../dist/fonts static/static",
    "start": "docusaurus-start",
    "build": "npm run prestart && docusaurus-build",
    "publish-gh-pages": "npm run prestart && docusaurus-publish",
    "write-translations": "docusaurus-write-translations",
    "version": "npm run prestart && docusaurus-version",
    "rename-version": "docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.3.2",
    "remarkable-embed": "^0.4.1"
  },
  "dependencies": {}
}
