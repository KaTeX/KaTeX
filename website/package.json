{
  "scripts": {
    "prestart": "node lib/build.js",
    "start": "docusaurus-start",
    "build": "yarn prestart && docusaurus-build",
    "publish-gh-pages": "yarn prestart && docusaurus-publish",
    "write-translations": "docusaurus-write-translations",
    "version": "yarn prestart && docusaurus-version",
    "rename-version": "docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.3.2"
  },
  "dependencies": {}
}
